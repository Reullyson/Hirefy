services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: hirefy-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
      - DATABASE_URL=sqlite:///db.sqlite3
    networks:
      - hirefy-network
    restart: unless-stopped

  scrapper:
    build:
      context: ./scraping
      dockerfile: Dockerfile
    container_name: hirefy-scrapper
    ports:
      - "5000:5000"
    volumes:
      - ./scraping:/app
    networks:
      - hirefy-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: hirefy-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - hirefy-network
    restart: unless-stopped

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    container_name: hirefy-nginx
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend
      - scrapper
    networks:
      - hirefy-network
    restart: unless-stopped

networks:
  hirefy-network:
    driver: bridge
